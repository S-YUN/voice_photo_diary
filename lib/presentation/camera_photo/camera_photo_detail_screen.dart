import 'package:flutter/material.dart';

class CameraPhotoDetailScreen extends StatelessWidget {
  const CameraPhotoDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}